name: Setup workspace
description: Install Node (pinned by .nvmrc) and workspace dependencies, with npm cache.

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version-file: .nvmrc
        cache: npm

    - name: Install dependencies
      shell: bash
      run: npm ci
